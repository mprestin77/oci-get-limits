# One OCI service name per line.
# Blank lines and lines starting with # are ignored.
#
# Examples:
#   python3 get-limits-service-usage.py --services-file services.conf
#   python3 get-limits-service-usage.py --services-file services.conf --only-with-usage

identity
certificates
batch-computing
vcn
load-balancer
faas
compute
